'use strict';

class FileWriter {
  constructor(path) {
    this.path = path;
  }

  write(data, callback) {
    // get path from data
    // write data to file with callback
  }
}

module.exports = FileWriter;
